# backlot-64 budgets.  `make check` measures every line here in VICE (both
# REU tiers, the larger value counts) and fails when a number is over its
# limit, when a line has no measurement, or when a measurement has no line.
# Limits sit about 5 percent over what was measured on 2026-09-18: the
# benchmark's start is not locked to the raster, so its counts move by a
# percent or two from run to run.  Raising a limit is a decision: say why in
# the commit.
#
# name                      limit   measured  what it protects
engine.bytes                 9971   #  8797   CODE + RODATA inside $080D-$2EFF, the resident engine (the page at $2F00 is the modules' entry table); 7,498 before the module manager and SYS
lowram.bytes                 1536   #  1044   engine tables in $0200-$07FF (608 before the module manager: its table is 400 bytes)

bench.fetch_64_border         180   #   169   one sprite frame: setup plus 64 bytes
bench.fetch_256_border        380   #   361   one script page (a page cache miss)
bench.fetch_1k_border        1190   #  1129
bench.fetch_4k_border        4420   #  4201
bench.fetch_8k_border        8850   #  8423   badlines included: 8 KB does not fit the border
bench.fetch_64_display        180   #   169
bench.fetch_256_display       380   #   361
bench.fetch_1k_display       1280   #  1213
bench.fetch_4k_display       4810   #  4579
bench.fetch_8k_display       9510   #  9053
bench.vm_64_loop             7950   #  7561   118 cycles per LOOP
bench.vm_drive_71            6170   #  5868   83 cycles per opcode in the drive body
bench.vm_add_65              6250   #  5952   92 cycles per ADD
bench.asm_drive_64          14400   # 13712   the same drive step in assembly: 214 cycles

# The scroller's prepare for one cell crossing, interrupts off, started
# inside the display (roadmap step 6, 2026-09-18).  The diagonal is the
# worst case and its limit is the target itself.  Before the fills work the
# same crossings cost 4,609, 4,281 and 6,341.
bench.scroll_right           4450   #  4218   shift, column, stash
bench.scroll_down            4350   #  4124   shift, row, stash
bench.scroll_diag            6000   #  5754   shift, column, row, stash: the target

# The multiplexer (roadmap step 9, then 32 sprites in Phase 2.5): b64_spr_end
# for the harness's 32 sprites, interrupts off, the first list (which
# generates the interrupt routines) and the next frame's; and the interrupt
# cycles per frame in the harness, both handlers, the blank's other work and
# the status-row split included.  The harness puts a sprite every 4 lines,
# the densest the hardware can reuse with seven sprites.  The previous
# multiplexer, at 24 sprites: 7,329 / 8,172 and 8,891.  Raised 2026-09-18
# (14,489 / 7,462 before): the allocation now models when the chain is free,
# since a group cannot start until the one before it has finished, which
# costs about 45 cycles a sprite; without it the 64-sprite layout wrote about
# 40 entries a frame late at 1 MHz (tools/b64muxhw.py).
bench.mux_first             16300   # 15524
bench.mux_next               9020   #  8586   268 a sprite: sort, allocate, reject, group
mux.irq_frame                8850   #  8389   32 sprites, one group every 4 lines

# The module manager (docs/MODULES.md section 9, 2026-09-19): examples/modules,
# the game in p-code.  Ticks lost from frame 100 to 600 on a stock machine,
# with six people under the AI, a parked car, three loads (two of them
# swaps) and two refusals; and one swap, the water module over the ground
# module: the 6.5 KB fetch and the physics' resume (every body's map cache
# again), from the manager's load to its return.
modules.frames_lost            95   #    90
modules.swap_cycles         20500   # 19497

overlay.load_6k              7250   #  6881   a 6 KB code overlay into region A: 6,335-6,881 over six runs, as more or less
                                              # of it overlaps the display's badlines (8 KB until 2026-09-18: 9,097)

# The tick depends on the art: re-quantising the cruiser with the corrected
# luma table (2026-09-18) found wheels in four bottom sprites instead of
# three, and the fourth animated sprite added about 475 cycles (8,414 before).
# The new multiplexer (roadmap step 9, 2026-09-18) decides in the main loop
# what the old one did in interrupts, so the cutscene's main-loop tick rose
# by ~1,030 cycles (8,889 -> 9,924) while its interrupts fell by about as
# much; with 24 sprites the frame as a whole saves ~4,000 (see mux.* below).
# A tick that draws text (about 7,300 cycles) and a shimmer step in the same
# frame overran it (21,087, 2026-09-18, when the multiplexer's cost moved the
# scene a frame against the shimmer's phase); the step now waits a frame then.
cutscene.tick_median        10450   # 10007   callback plus engine frame work, interrupts included (through the cutscene module's jump table)
cutscene.tick_worst         17500   # 17057   the same on a shimmer frame (every fourth); a frame is 19,656

# The scroller in the autodrive example, interrupts included: the prepare's
# worst frame, and frames with no callback in 800.  Frames drop because the
# example's callback (traffic, the sprite sort, the debug numbers) plus the
# prepare overruns on crossing frames; roadmap step 9 is the fix, and the
# target is 0.  Before the fills work: 10,361 and 100.
scroll.worst_frame           8150   #  7750   scroll prepare, worst frame of the autodrive square
scroll.frames_dropped          95   #    89   frames without a callback, of 800

# The traffic demo (examples/traffic, 2026-09-18), driving itself on the stock
# tier: frames without a callback in 3,000.  It depends on the route, which
# depends on when VICE starts: 20 to 140 in runs of 3,000 (0.7 to 4.7 percent),
# mostly frames where a spawn step, a crossing's planning and the scroller's
# fill coincide.  The scroller example alone loses 52 in 800.
traffic.frames_lost           150   #    22   the demo's own measure: see above


# The physics module (modules/physics, docs/PHYSICS.md, 2026-09-18) in
# examples/physics after its tape: the player on foot, four walkers, four
# parked vehicles asleep.  The step's cycles include the interrupts that land
# in it.  Frames without a callback in 760.  Measured after 690 frames at
# first (4,521, 6,551, 51); the tape now ends with shooting and runs to 760,
# with more bodies awake when it is measured: the same code, a busier scene.
physics.step_median          6750   #  6358   phys_step over all 800 steps of the tape (was 24 quiet steps after it)
physics.step_worst          16200   # 15000   a three-car pile-up: two pairs resolved in one step
physics.frames_lost            42   #    39   of 800 (81 before the multiply shortcuts; the status row made and shown on separate frames)
boats.step_median            4100   #  3885   phys_step at the coast, over all 700 steps of the tape, either module in
boats.step_worst            10250   #  9680
boats.frames_lost               6   #     2   of 700, the two module swaps included
sky.step_median              3350   #  3159   phys_step with a helicopter, over all 700 steps of the tape, either module in
sky.step_worst               7050   #  6690
sky.frames_lost                 6   #     2   of 700, the two module swaps included
hover.step_median            1950   #  1824   phys_step, a hovering helicopter, a car and the player, over 600 steps
hover.step_worst             2750   #  2605
hover.frames_lost               2   #     0   of 600
plane.step_median            3350   #  3163   phys_step with a plane, over 880 steps, either module in
plane.step_worst             8350   #  7924
plane.frames_lost               6   #     2   of 880, the two module swaps included
debris.step_median           8350   #  7940   phys_step, the city's bodies, over 560 steps
debris.step_worst           15750   # 14993
debris.col_median             260   #   238   shot_step: shots, grenades and debris, over 560 steps
debris.col_worst             7450   #  7095   a blast: bodies pushed and six pieces thrown
debris.frames_lost             77   #    73   of 560: a city frame has no time to spare for six more sprites
marsh.step_median            4800   #  4558   phys_step, an airboat, a speedboat and the player, over 400 steps
marsh.step_worst             7750   #  7387
marsh.frames_lost               2   #     0   of 400
crowd.ai_median              6250   #  5933   AI_STEP, nine brains, over 420 ticks of the crowd scene
crowd.ai_worst              20550   # 19556   a tick with two sight traces and a burst of decisions
crowd.step_median           14500   # 13820   phys_step with ten bodies all moving
crowd.frames_lost             550   #   523   over 420 ticks: the scene runs at about 20 ticks a second
