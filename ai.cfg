# backlot-64 AI module: region A above the collision module, to the
# multiplexer's tables at $9800 (docs/AI.md, modules/ai/ai.inc).
MEMORY { AIM: start = $8D00, size = $0B00, file = %O; }
SEGMENTS { OVERLAY: load = AIM, type = ro; }
