# backlot-64 example: the game's own module (examples/modules/ops.s), in the
# top half of the game's resident area; the game's code keeps below $3800.
MEMORY { OPS: start = $3800, size = $0800, file = %O; }
SEGMENTS { OVERLAY: load = OPS, type = rw; }
