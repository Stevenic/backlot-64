# backlot-64 cutscene module: region A while a scene plays, fetched whole by
# b64_cut_begin (CUT_SIZE in include/b64.inc): the link refuses it if it grows
MEMORY { CUT: start = $8000, size = $0E00, file = %O; }
SEGMENTS { OVERLAY: load = CUT, type = ro; }
